$kind: http-request
url: "{{base_url}}/api/admin/users?role=DOCTOR"
method: GET
headers:
  Authorization: Bearer {{admin_token}}
queryParams:
  role: DOCTOR
scripts:
  - type: afterResponse
    code: pm.test('Status 200', () => pm.response.to.have.status(200));
    language: text/javascript
order: 2000
