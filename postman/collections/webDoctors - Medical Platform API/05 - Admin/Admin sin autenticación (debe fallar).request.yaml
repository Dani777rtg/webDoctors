$kind: http-request
url: "{{base_url}}/api/admin/users"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status 401/403 sin token', () => {
        pm.expect(pm.response.code).to.be.oneOf([401, 403]);
      });
    language: text/javascript
order: 7000
