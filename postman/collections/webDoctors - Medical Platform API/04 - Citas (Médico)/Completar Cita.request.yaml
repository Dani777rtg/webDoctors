$kind: http-request
url: "{{base_url}}/api/appointments/{{appointment_id}}/complete"
method: PUT
headers:
  Authorization: Bearer {{doctor_token}}
scripts:
  - type: afterResponse
    code: pm.test('Status 200', () => pm.response.to.have.status(200));
    language: text/javascript
order: 3000
